<html>
   <head>
      <title>Page Title</title>
      <meta charset="UTF-8">
      <meta name="viewport" content="width=device-width, initial-scale=1">
      <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 
            viewBox=%220 0 100 100%22><text y=%22.9em%22 font-size=%2290%22>✉</text></svg>">
<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
      <style>
         * {
            box-sizing: border-box;
         }
         :root{
            --animation-animate-to: 50%;
         }
         /* Hide scrollbar for Chrome, Safari and Opera */
         .body::-webkit-scrollbar {
            display: none;
         }

         /* Hide scrollbar for IE, Edge and Firefox */
         .body {
            -ms-overflow-style: none;  /* IE and Edge */
            scrollbar-width: none;  /* Firefox */
         }
         /* Style the body */
         body {
            font-family: Arial;
            margin: 0;
            overflow-y:hidden
         }

         /* style the container */
         .login-container {
            position: relative;
            border-radius: 5px;
            padding: 20px 0 30px 0;
         } 
         /*color accessible placeholder */
         ::-webkit-input-placeholder {
            color: black;
            opacity: 1;
         }

         :-moz-placeholder { /* Firefox 18- */
            color: black;
            opacity: 1;
         }

         ::-moz-placeholder {  /* Firefox 19+ */
            color: black;
            opacity: 1;
         }

         :-ms-input-placeholder {
            color: black;
            opacity: 1;
         }
         input,
         .btn {
            width: 100%;
            padding: 12px;
            border: 4px solid #c0ded9;
            margin: 5px 0;
            display: inline-block;
            text-align: center;
            letter-spacing: .15em; /* Adjust as needed */
            text-decoration: none; /* remove underline from anchors */
            color: black;
            background-color: white;
            font-size: 16px;
            font-weight: 600;
         }

         input:not([type="submit"]):focus {
            background-color: #b2c2bf;
         }

         input:not([type="submit"]):focus::-webkit-input-placeholder
         {
            opacity: 0;
         }

         input[type=submit] {
            background-color: #c0ded9;
         }

         input[type=submit]:active {
            background-color:  #3b3a30;
            color: white;
         }


         /* Header/logo Title */
         .header {
            padding: 10px;
            text-align: center;
            color: #3b3a30;
            margin: 0; /* Gives that scrolling effect as the typing happens */
            letter-spacing: .15em; /* Adjust as needed */
            overflow: hidden;
            white-space: nowrap; /* Keeps the content on a single line */
            padding: 5px 5px 5px 5px;
            font-size: 150%;
            margin-left: -0.5em;
         }

         .main-header
         {
            font-size: 200%;
            margin-left: 0;
         }

         /* Column container */
         .row {  
            display: flex;
            flex-wrap: wrap;
            height: 100vh;
         }

         /*Create a column that takes up 50% of its containers width and 100% of its height*/
         .flex-div-50 {
            flex: 50%;
            background-color: white;
            padding: 20px;
            min-width: 50%;
            height: 100%;
            display: flex;
            justify-content: center;
            align-items: center;
            flex-wrap: wrap;
            flex-direction: column;
         }

         /*When the screen is less than 700px wide, make the two columns stack on top of each other instead of next to each other */
         @media screen and (max-width: 700px) {
            .row{   
               flex-direction: column;
            }
            :root{
               --animation-animate-to: 100%;
            }
         }

         @media (orientation: landscape) {
            :root{
               --animation-animate-to: 100%;
            }
         }

         @media screen and (max-device-aspect-ratio: 1/1), (max-aspect-ratio: 1/1)
         {
            .row{   
               flex-direction: column;
            }

            .body
            {
               overflow-y: scroll;
            }

            .flex-div-50
            {
               width: 100vw;
            }
         }

         .typewriter-effect {
            border-right: .15em solid #c0ded9; 
            animation: typing 10s steps(30, end) infinite, blink-caret .85s step-end infinite;
         }


         /* The typing effect */
         @keyframes typing {
            from, 78%, 100%{ width: 0; visibility: hidden; display: none;}
            40%, 50%, 60% {width:var(--animation-animate-to); visibility: visible; display: block; }


         }


         /* The typewriter cursor effect */
         @keyframes blink-caret {
            from, to { border-color: transparent }
            50% { border-color: #c0ded9; }
         }

      </style>
   </head>
   <body>

      <!-- Note -->

      <!-- The flexible grid (content) -->
      <div class="row">
         <div class="flex-div-50">
            <h1 class="header main-header"> Beagle Omegle</h1>
            <h1 class="header typewriter-effect"> Talk Anonymously.</h1>
         </div>
         <div class="flex-div-50">
            <div class="login-container">
               <h1 class="header main-header"> Login</h1>
               <form action="/api/auth/login" method="post">
                  <input id="user_id" type="text" name="user_id" placeholder="Username" aria-label="username" required>
                  <input id="password" type="password" name="password" placeholder="Password" aria-label="password" required>
                  <input type="submit" value="Login">
               </form>

            </div>
         </div>
      </div>

   </body>
</html>

